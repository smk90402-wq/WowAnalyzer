requests>=2.31
python-dotenv>=1.0
pandas>=2.0
scikit-learn>=1.3
numpy>=1.24

# 마이그레이션 후: web UI (PySide6 폐기)
fastapi>=0.110
uvicorn[standard]>=0.27
pywebview>=6.0
pythonnet>=3.1     # pywebview 6.x 가 Windows EdgeChromium backend 위해 강제

# 다중 사용자 (web 모드): ID/PW 인증
bcrypt>=4.0         # 패스워드 해시 (passlib 4.x 호환 이슈 회피, 직접 사용)
itsdangerous>=2.1   # signed session cookie
