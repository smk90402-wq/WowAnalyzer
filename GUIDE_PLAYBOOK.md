# 스펙 가이드 제작 플레이북

최상위 로그 기반 스펙 가이드(딜사이클 플로우 + 스탯/장신구 + 보스별 운용 + 올100 로드맵)를 만드는 표준 절차.
냉법(2026-07-03)·야수냥(2026-07-04) 제작 과정에서 확립. **대충 분석 금지** — 아래 함정 목록과 검증 게이트는 전부 실제 사고에서 나왔다.

## 0. 전제

- 대상 독자: 로그 최고점(99~100)을 노리는 레이더. 성능 순위가 아니라 "따라 하면 되는" 가이드.
- 정답지 = 최상위 로그의 실제 행동 + 그 이유(특성/스킬 메커니즘). 이론 가이드와 실측이 다르면 왜 다른지까지 규명.
- 문장은 쉬운 한국어. 레이드 전용(쐐기 무시). 개인 메모 금지(불특정 다수 열람 가능).

## 1. 데이터 수집 (순서 중요)

| 단계 | 명령/파일 | 비고 |
|---|---|---|
| 오늘자 랭킹 | `python fetch_rankings_v2.py 5` (+`4` 영웅) | 분석 전 항상 최신화 — 옛 명단 캐시로 분석하면 표본이 붕괴함 |
| 특성/장비 | backfill 래퍼: `PRIMARY_TARGETS={(클래스,스펙)}` 후 `main(missing_only=True, no_events=True)` | 9보스×top100 = 900행 |
| 캐스트/버프 | `main(per_spec=300, missing_only=True)` | 로테 분석은 300킬이면 충분 |
| 특성 트리 | `fetch_talent_trees.py` SPECS에 스펙 추가 후 실행 | ★정점 특성 = spec 트리 최하단 행(row 10±)에 포함됨★ |
| PI 플래그 | `fetch_pi_all.py 5` | ⚠backfill_v2와 동시 실행 금지 — V2 캐시를 서로 덮어씀. 순차로 |

## 2. 분석 차원 (각각 data/*.json 산출 + 스크립트 커밋)

1. **스탯/장신구**: `analyze_{spec}_stat_recommendations.py` / `analyze_{spec}_trinket_recommendations.py` (BM·Frost판 복제. 장신구 ID는 하드코딩 말고 착용 데이터에서 자동 수집)
2. **보스별 실측 사이클**: `extract_boss_dealcycle.py` SPEC_CONFIG에 스펙 추가(쿨기/추적버프 ID는 spell_db에서 한글명으로 찾아 실제 캐스트와 교차 확인 후) → 딜사이클 탭 '보스별 기록' 모드
3. **4차원 채굴** (워크플로 병렬):
   - 프록/교대 규율 — 버프 apply→소모 지연, 낭비(겹침/만료), 밴드(1~20 vs 21~100) 비교
   - 쿨기 홀드 — 보스별 사용 시각·재사용 간격, 웨이브 정렬, 오프너 첫 사용
   - 특성 분기 — 보스별 top20 vs rest 채택률, 유틸 스왑, 함정 노드
   - parse100 갭 — 1~5등 vs 21~100등: 실행 지표(분당 시전·다운타임) / 킬타임 / PI. "100점에 실행 말고 뭐가 필요한가"
4. **프록 소스 인벤토리**: 특성+영웅+정점+**티어 세트**+**장신구** 전 소스의 프록/강화 목록 (`proc_sources_{spec}.json`) — 추적 버프 섹션의 근거

## 3. 함정 목록 (전부 실제 사고)

- **빈도≠우선순위**: "필러의 54%가 코브라" ≠ "코브라 우선". 충전/자원 상태를 조건으로 나눠 재검증할 것. (코브라 사건)
- **행동≠이유**: 채굴은 '무엇을'만 말해줌. '왜'는 반드시 특성 트리·스킬 설명에서 — 같은 효과를 주는 더 큰 특성이 따로 있을 수 있음. (전쟁 명령 -3초 놓치고 코브라 -1초를 이유로 쓴 사건)
- **대표 시퀀스 착시**: 표본 적은 보스의 최빈 1판을 오프너 대표로 삼으면 소수파가 정답처럼 보임. (바엘고어 6킬 '격노→난타' 오탐 — 진실은 난타 먼저 66~79%)
- **top100 중앙값은 배정형 특임을 못 봄**: 특임 맡은 킬은 애초에 상위 파스가 안 됨 — 배정형 특임은 통계가 아니라 큐레이션으로.
- **펫 버프는 플레이어 buffs에 없음**: 광기 같은 펫 오라는 측정 불가로 정직하게 표기.
- **구버전 이론 노트 주의**: 리서치 노트가 개편 전 내용일 수 있음 — 채택률 0%면 죽은 기술. (혜성 폭풍: 노트엔 '쿨마다 최우선', 실측 301킬 시전 0회)
- **캐시-명단 정합**: 분석 대상은 항상 '오늘자 rankings CSV에 있는 킬'로 필터.
- **팬 번역명 함정**: 스킬·버프 이름은 반드시 spell_db/트리의 공식 한글명으로 검증 — 유저가 게임/위크아우라에서 못 찾는 이름은 가이드 실격. (실제 적발: 시듦의 불길→부패의 사격, 야수 휩쓸기→야수의 회전베기, 빙결의 비→빗발치는 냉기, 무리 우두머리의 울부짖음→무리의 지도자의 포효)
- **전 확팩 메커니즘 잔재**: 툴팁·트리에 근거 없는 관리 규칙(예: '날사로 광기 3중첩 유지')은 구버전 기억일 수 있음 — 현 데이터에서 확인 안 되면 삭제. (광기는 이번 시즌 날사와 무관 — 광포한 야수 소환 버프였음)
- **충전제 완충 효과**: 쿨감 계산은 충전 수까지 넣어야 함 — 살상 명령 2충전이 날카로운 -3초의 넘침을 흡수해 필러 순서 손익이 ~0이 된 사례. 단순 '초당 이득' 비교는 틀림.

## 4. 검증 게이트 (출시 전 의무)

1. **체크리스트 ↔ 문제풀이(rotgame.js) 룰 대조** — 트레이너가 실행 가능한 정답지. (냉법 초안에서 순서 오류 5건 적발)
2. **메커니즘 감사 워크플로** — 가이드의 모든 "X가 Y를 준다/깎는다/몇 초/몇 %" 문장을 트리+스킬DB와 대조, 소견마다 반박 검증. 근거 못 찾으면 '확인 불가'로.
3. **이론 vs 실측 불일치 규명** — 다르면 가설 세우고 데이터로 판정(버프 조건부 분석 등). 판정 안 되면 '미해결' 정직 표기.
4. 리뷰 워크플로 산출물(*.json)과 tmp_mine_*.py는 커밋 — 근거 추적/재현 가능하게.

## 5. 콘텐츠 형식

**rotation_data.json build.flow 스키마**: `note`(우선순위 framing + 스펙 핵심 원칙) / `checklist[]`(q=상황체, a=버튼, why, tone: proc|cd|hold|spend|filler) / `aoe_diff[]`(광역은 차이점만) / `opener_single[]`·`opener_aoe[]`(아이콘 타임라인, 캡션 한 줄) / `opener_note` / `tracking{always[],sometimes[]}`(추적 버프 — 프록 소스 인벤토리 기준) / `util_tips{title,items[{s,n,macro}]}`(매크로는 macro 필드 → 복사 블록) / `glossary[]`.

**문장 규칙** (사용자 확정):
- 표 성격 명시: "위에서부터 우선순위입니다 — 쿨과 조건이 맞는 것 중 가장 위" ("처음 예인 줄" 같은 기계 표현 금지)
- 스펙 핵심 원칙을 note 첫머리에 (예: "자연의 동맹 때문에 필러→살상 교대")
- "주력 딜링기"(제일 아픈✗) / "쿨 돌 때마다"(쿨이 돌아왔다✗) / "쓰기 전에"(진입 전✗) / "전투 카운트 2쯤에 먼저"(선시전✗) / "실제 로그"(실측✗)
- 수치는 판단에 필요한 것만. 꿀팁은 무엇을→왜→어떻게까지(처음 보는 사람 기준). 통계용어 금지.
- 갈리는 결론은 갈림 자체를 표기. 확인 안 된 건 쓰지 않는다.

**UI 규칙**: 상세는 중앙 팝업(spec-modal 패턴), 표 아래 배치 금지. 텍스트 드래그 복사 유지(`text_select=True`). 데이터(junction)만 바뀌면 빌드 불필요, `app/`·`serve.py` 변경 시 즉시 `build.bat`.

## 6. 완성 체크리스트

- [ ] 오늘자 수집(랭킹→pf→events→트리→PI 순차)
- [ ] 스탯/장신구/보스별/4차원/프록 인벤토리 산출
- [ ] flow 작성 (문장 규칙 준수)
- [ ] 게이트 1~3 통과 (트레이너 대조 · 메커니즘 감사 · 불일치 규명)
- [ ] spec_guide.json 팁 (실측 근거 + 출처 명시, 보스별 운용 포함)
- [ ] 프리뷰 검증 → 빌드 → 커밋(EOL 정리) → 푸시
