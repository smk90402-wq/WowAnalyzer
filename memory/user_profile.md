---
name: user profile
description: User's language, interests, and technical context
type: user
---

- Communicates in Korean — respond in Korean by default
- WoW player, familiar with raid/log/spec terminology (uses Korean WoW jargon like "네임드", "한밤" for Midnight expansion, "외부 버프 제외")
- Python is installed locally
- WarcraftLogs V1 API key: user has been warned multiple times about chat exposure and **explicitly chose not to rotate it** — they reuse the same key across multiple working environments. Do NOT remind them to rotate the key anymore. Still enforce `.env` stays gitignored so the key doesn't get committed.
